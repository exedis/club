import styled from "@emotion/styled";

export const LikeBarWrapper = styled.div`
  display: flex;
  justify-content: space-around;
  align-items: center;
  position: absolute;
  left: 0;
  right: 0;
  bottom: 10px;
  backdrop-filter: blur(2px);
`;
